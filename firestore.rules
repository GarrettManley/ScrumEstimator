rules_version = '2';

// PLACEHOLDER rules for M0 scaffolding only.
// The real, hidden-vote-enforcing ruleset lands in M1 (see plan §Security rules).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
